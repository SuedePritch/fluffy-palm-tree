<a href="/postform" id="new-post-button">NEW POST</a>
    {{#each projects as |project|}}
    <a class="card" target="_blank">
        <div class="title"  id={{project.id}}>
            <p id="title-text">{{project.title}}</p>

        </div>
                <p class="content-text" id="desc-text">{{project.description}}</p>
            {{!-- <div class="tech-container">
                {{#each tags as |tag|}}
                <img src={{tag.tech_icon}} id="tech-icon">
                {{/each}}
            </div> --}}
                {{!-- <p id="username-text">{{post.user.username}}</p> --}}
                <p class="content-text" id="rate-text">${{project.budget}}</p>
    </a>
    {{/each}}